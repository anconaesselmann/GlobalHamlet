import UIKit
import XCTest
/*
class ConnectionsTest: XCTestCase {
    var sut : Connections!
    
    override func setUp() {
        super.setUp()
        sut = Connections()
    }
    
    override func tearDown() {
        super.tearDown()
    }
    
    func test_didReceiveAPIResults() {
        let connection1 = [
        
        
        ]
        let response = [[],[]]
        letresults = NSDictionary(dictionary: ["response": response, "errorCode": 0] )
        let initDict = ["show_user_name": false, "show_real_name": true, "show_alias": true]
        let expectedDict = ["show_user_name": true, "show_real_name": false, "show_alias": false]
        sut.setSwitches(initDict)
        sut.setSwitch("show_user_name", value: true)
        XCTAssertEqual(expectedDict, sut.getSwitches())
    }
}*/